let num = 10    // 0 = False / 1 = True
                 // verdadeiro / falso
res = (!(num % 2) ? "Par" : "Impar")      // '?' é o operador ternario, '!' nega 'num % 2' retornaria 'false'

console.log(res)        // retorna 'true' no console